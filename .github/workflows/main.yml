name: test hands on 

on:
  push:
    branches: [ "main" ]
  pull:
    branches: [ "main" ]
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: checkout-code
        uses: actions/checkout@v4
      - name: set up nodejs
        uses: actions/setup-node@v4
      - name: install dependencies
        run: npm install
   
        
      
        
